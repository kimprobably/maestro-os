# Day 0 Checkpoint

## Local Setup

- `maestro-os` initialized as a Git repo on `main`.
- `fabro-maestro` cloned as a sibling repo.
- `fabro-maestro` working branch: `spike/openrouter-slack-gates`.
- Missing local tools identified before setup: Rust/Cargo, Graphviz `dot`, Daytona CLI.
- Installed local tools:
  - Rust/Cargo: `1.95.0` via Homebrew.
  - Daytona CLI: `v0.173.0` via Homebrew.
  - Graphviz: `14.1.5` via Homebrew.
- Graphviz is not linked into `PATH` because Homebrew detected a `gc` binary conflict with `gascity`; use `/opt/homebrew/opt/graphviz/bin/dot` until that conflict is intentionally resolved.
- `cargo build --release` completed for `fabro-maestro` in `27m33s`.
- Built binary reports `fabro 0.231.0-nightly.1 (d6d7f18 2026-05-12)`.
- Debug binary is available on PATH through `~/.local/bin/fabro`, pointing at the active `fabro-maestro` workspace build.

## Human Setup Required

Day 0 cannot be completed fully by Codex because the plan requires browser/account setup:

- Run `fabro server start` and complete the browser wizard.
- Create the Neon project and paste the clean `DATABASE_URL` into `.env.local`.
- Create/install the Slack app, enable Socket Mode, and paste clean Slack tokens into `.env.local`.
  - Fabro expects `FABRO_SLACK_APP_TOKEN` for the `xapp-...` app-level Socket Mode token.
  - Fabro expects `FABRO_SLACK_BOT_TOKEN` for the `xoxb-...` bot token used by `chat.postMessage` and `chat.update`.
  - A Slack signing secret is not needed for Fabro Socket Mode.
  - Slack default channel for the spike: `C0AHCRH4EP4`.

Fabro server config snippet:

```toml
_version = 1

[server.integrations.slack]
enabled = true
default_channel = "C0AHCRH4EP4"
```
- Create or provide Daytona API access, then run the sandbox auth test in `sandbox/README.md`.
- Add `OPENROUTER_API_KEY` to `.env.local` before the OpenRouter smoke test.

## Decisions Applied

- Slack workspace: use the plan default, a dev workspace named `maestro-dev`.
- Sandbox: use Daytona for the spike.
- Initial ICP: use the plan default, fintech Series A-B.
- Voice guide: extract from the plan-listed source materials unless a canonical voice doc is provided.
- Persona avatars: emoji-only for the spike.
- Rig is an output app-framework candidate for Rust AI apps generated by Fabro. It does not replace Fabro.
- cortex-mem is the leading memory-sidecar candidate. Use MCP/REST first, with durable writes controlled by a memory curator instead of arbitrary worker agents.

## Notes

- Daytona CLI `v0.173.0` accepts `daytona snapshot create <name> --dockerfile <path> --context <path> --cpu <n> --memory <gb> --disk <gb>`; it does not use the TOML snapshot sketch from the plan.
- Local Claude/Codex auth directories should not be committed or uploaded as a snapshot build context.

## Rust Progress

- OpenRouter provider support is implemented in `fabro-maestro` on branch `spike/openrouter-slack-gates`.
- Slack human-gate cards now emit explicit `approve` / `reject` / `edit` gate-decision payloads for approval-style options.
- Slack button blocks now use unique `action_id` values per button, required by Slack Block Kit.
- Slack posting failures are now logged instead of being silently ignored.
- Socket Mode dispatch now routes gate decisions separately from generic interview answers.
- Server route added: `POST /api/v1/runs/{id}/gates/{gate_id}/decision`.
- Slack `chat.postMessage` request building now supports persona-style `username` and `icon_url` overrides for `chat:write.customize`.

## Phase 1 Test 2 Local E2E

- Run ID: `01KREXTYHDWJ19RBFAYNR6YWR4`.
- Sandbox: `local` for this smoke run; Daytona remains a separate sandbox test.
- OpenRouter prompt stages succeeded with `anthropic/claude-haiku-4-5`.
- Slack approval card posted to `C0AHCRH4EP4` with action IDs `gate.decision.0` and `gate.decision.1`.
- Approval was submitted through the Fabro gate-decision API after confirming the Slack card was visible. This validated the server decision path and Slack `chat.update`; a physical Slack button click through Socket Mode is still pending.
- Workflow completed successfully in `3m20s`.
- The command stage invoked `bin/maestro slack post` and posted: `Workflow complete: Fabro ran the command-stage Maestro CLI path.`

## Phase 1 Test 2 Physical Slack Button E2E

- Run ID: `01KRF0HE119FJ2JS9NE6ES60XA`.
- Slack card timestamp: `1778620351.323109`.
- Tim clicked `[A] Approve` in Slack.
- Socket Mode received the interaction and Fabro emitted `interview.completed` with answer `A`.
- Run unblocked at `2026-05-12T21:17:05Z`.
- Workflow completed successfully at `2026-05-12T21:17:09Z`.
- This closes the physical Slack button-click gap for Phase 1.

## Phase 1 Test 4 Iteration Speed

- Run ID: `01KREZ5ZQEQ66WPXWBQWVYXRY0`.
- Test edit: changed the Plan prompt to require the exact marker `ITERATION-SPEED-CHECK`.
- Measurement: edit -> first-stage output visible in Slack completed in `5s`.
- Slack approval cards now include the prior stage output as a `Context` section, so the stage result is visible at the approval point instead of requiring event-log inspection.
- The temporary prompt marker has been removed from `workflows/phase1/test-run.fabro`; the run evidence remains in Fabro events and Slack history.

## Phase 1 Test 5 Sub-Workflow Invocation

- First-class path exists in Fabro as `type="stack.manager_loop"` with `stack.child_workflow`.
- Added `workflows/phase1/subflow-wrapper.fabro` and `workflows/phase1/subflow-child.fabro`.
- Validation passed for both workflows.
- Run ID: `01KREZG8WKCR3QM8S7RMV1G3D3`.
- Result: wrapper completed successfully in `2s`.
- Data flow verified: child emitted `subflow.result=CHILD-SUBFLOW-CHECK result=ok`; parent prompt preamble included that context key and emitted `PARENT-SAW-CHILD result=ok`.
- Implementation note: `response.*` and `last_response` propagate in the manager-loop event context, but compact LLM preambles intentionally hide them. Child workflows that need parent-visible output should write explicit user keys such as `subflow.result`.

## Local Verification

- `cargo test -p fabro-slack --no-fail-fast` passed.
- `cargo test -p fabro-model` passed.
- `cargo test -p fabro-auth configured_providers_reads_injected_env` passed.
- `cargo test -p fabro-llm from_credentials_supports_openrouter_provider` passed.
- `cargo test -p fabro-server gate_decision --no-fail-fast` passed.
- `cargo clippy -p fabro-slack -p fabro-server --all-targets -- -D warnings` passed with the local stable toolchain.
- `cargo check --workspace` passed.
- `git diff --check` passed.
- Release rebuild after the Slack action ID fix completed in `12m15s`.

## Verification Gaps

- The exact repo formatter command, `cargo +nightly-2026-04-14 fmt --check --all`, cannot run until `rustup` and the pinned nightly toolchain are installed.
- Live OpenRouter smoke test passed through the local Phase 1 Test 2 workflow.
- Slack post/update/API-approval E2E passed. Physical Slack button click through Socket Mode also passed in run `01KRF0HE119FJ2JS9NE6ES60XA`.
- Daytona sandbox creation and custom snapshot creation work with the broader API key. Active custom snapshot `maestro-agent-test-20260512` (`1047d5db-7497-4fb0-85b6-c2c2cc284722`) was created from `sandbox/Dockerfile`. Manual Claude/Codex login inside the default sandbox works and both CLIs respond non-interactively after login. The remaining gap is repeatable subscription auth state. See `sandbox/README.md` and `docs/PHASE-1-FINDINGS.md`.
