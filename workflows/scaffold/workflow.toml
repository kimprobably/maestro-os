_version = 1

[workflow]
graph = "scaffold-workflow.fabro"

[run]
goal = "Generate a toy Slack hello Fabro workflow from the scaffold spec"

[run.inputs]
spec = "specs/scaffold/toy-slack-hello.md"
workflow_path = "workflows/scaffold/generated-toy-slack-hello.fabro"
const_path = ".maestro/scaffold/constitution.md"
