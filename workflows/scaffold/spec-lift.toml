_version = 1

[workflow]
graph = "spec-lift-workflow.fabro"

[run]
goal = "Lift the toy Slack hello idea into a complete Maestro spec"

[run.inputs]
idea_path = "specs/scaffold/toy-slack-hello.md"
spec_path = "specs/scaffold/completed-toy-slack-hello.md"
spec_json_path = "specs/scaffold/completed-toy-slack-hello.json"

[run.sandbox]
provider = "local"
