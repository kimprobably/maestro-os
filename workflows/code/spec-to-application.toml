_version = 1

[workflow]
graph = "spec-to-application.fabro"

[run]
goal = "Build an application from an approved Maestro spec"

[run.inputs]
spec_path = "specs/scaffold/completed-toy-slack-hello.md"
app_dir = "apps/generated-app"
