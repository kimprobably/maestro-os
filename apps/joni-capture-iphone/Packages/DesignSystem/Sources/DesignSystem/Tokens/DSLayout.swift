import SwiftUI

/// Layout tokens for consistent spacing and sizing
public enum DSLayout {
    /// Max readable width for auth/onboarding stacks
    /// Provides optimal line length and button sizes on larger screens
    public static let readableMaxWidth: CGFloat = 420
}

