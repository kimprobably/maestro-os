# Supabase Edge Functions Configuration

[functions.ai]
verify_jwt = true  # Automatically verify Supabase auth tokens

[edge_runtime]
policy = "per_worker"

# Add CORS origins for your app
# Update with your production domain when deploying

