I have successfully drafted the UX Outcome spec candidate (`.workflow/enhancement-discovery/spec-candidates/ux-outcome.md`) based on the requested rubric.

The spec specifically addresses the key product feedback points and capability gaps by detailing:
- User workflows transitioning from fake/opaque layouts to live data transparent UI.
- All visible product changes and new API endpoints required (`/api/apps/seed`).
- Strict acceptance gates requiring real ingestion pipelines and velocity-based ranking.
- Hard non-cheating constraints that block mock data passing off as live data.
- Both a TDD and Eval-Driven Development plan to confirm UI fidelity and logic correct.
- A pairwise comparison highlighting benefits over data-first and workflow-first models.
- Step-by-step simplification steps to ship the value incrementally.

The stage is complete and the workspace is ready for the next actions.